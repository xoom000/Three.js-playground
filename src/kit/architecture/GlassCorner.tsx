import {DGAsset,type DGAssetProps} from '../core/DGAsset'
/** GlassCorner. Independent textured GLB. */
export function GlassCorner(props:DGAssetProps){return <DGAsset asset="GlassCorner" {...props}/>}

import {DGAsset,type DGAssetProps} from '../core/DGAsset'
/** LinearLight. Independent textured GLB. */
export function LinearLight(props:DGAssetProps){return <DGAsset asset="LinearLight" {...props}/>}

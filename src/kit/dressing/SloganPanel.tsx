import {DGAsset,type DGAssetProps} from '../core/DGAsset'
/** SloganPanel. Independent textured GLB. */
export function SloganPanel(props:DGAssetProps){return <DGAsset asset="SloganPanel" {...props}/>}
